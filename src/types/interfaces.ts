export interface Planets {
    id: number;
    title: string;
    color: string;
    type?: string;
}

export interface AuthData {
  email: string;
  password: string;
}

export interface GalaxyData {
  name: string;
  color: string;
}